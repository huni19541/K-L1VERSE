server:
  port: 7040

spring:
  profiles:
    active: deploy
  jpa:
    hibernate:
      ddl-auto: update

  # redis 설정
  data:
    redis:
      host: test_redis
      port: 6379
      password: switchlight2024!
